# Emoticons

Build every version/loader with `./gradlew buildAll`.

- **1.12.2 (Forge):** build with `./gradlew :1.12.2:forge:build` and test with `./gradlew :1.12.2:forge:runClient`.
- **1.10.2 (Forge):** build with `./gradlew :1.10.2:forge:build` and test with `./gradlew :1.10.2:forge:runClient`.
- **1.9.4 (Forge):** build with `./gradlew :1.9.4:forge:build` and test with `./gradlew :1.9.4:forge:runClient`.
- **1.8.9 (Forge):** build with `./gradlew :1.8.9:forge:build` and test with `./gradlew :1.8.9:forge:runClient`.
- **1.7.10 (Forge):** build with `./gradlew :1.7.10:forge:build` and test with `./gradlew :1.7.10:forge:runClient`.
